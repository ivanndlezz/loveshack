# Plan: Fix Theme Toggle Functionality and Adjust Header Design

## Objective
Fix the theme toggle button so it properly switches between light and dark modes when clicked, and update the header layout to be more space-efficient with a logo image and simplified toggle design.

## Current State Analysis
- The theme toggle button is now in the header but clicking it does nothing due to event listener timing issues (header is rendered dynamically after page load).
- The current toggle includes text label and pill indicator, taking up significant space.
- Header structure uses left/title/actions layout, but could be more compact.
- Header title is text "⛵ Love Shack", should be replaced with logo image.

## Issues Identified
1. **Functionality**: Theme toggle event listener is attached on page load but the button is created dynamically, so no click handler is bound.
2. **Design**: Toggle is too verbose with text and pill, not suitable for header space.
3. **Layout**: Header needs better organization and spacing for the new elements.

## Proposed Changes
1. **Fix Functionality**: Move theme toggle event attachment to app.js after header rendering.
2. **Simplify Toggle**: Remove text label and pill, keep only the icon for a compact design.
3. **Update Header Structure**: 
   - Change title to logo image
   - Use header-right container for actions
   - Improve spacing and alignment
4. **Update CSS**: Adjust styles for the new header layout and simplified toggle.

## Implementation Steps
1. **Move Theme Toggle JS**: Remove theme toggle code from index.html and add it to app.js in the navigate method after header update.
2. **Update Header HTML**: Modify renderDefaultHeader in app.js to match the provided structure with logo image and simplified toggle.
3. **Update CSS**: Add styles for header-right and adjust header-theme-toggle for icon-only design.
4. **Test Functionality**: Ensure theme switching works and visual design is appropriate.

## Files to Modify
- `v3/js/app.js`: Update renderDefaultHeader structure and add theme toggle event attachment after header rendering.
- `v3/index.html`: Remove theme toggle JS code from the bottom script.
- `v3/css/layout.css`: Add styles for header-right and update header-theme-toggle.

## Validation
- Click theme toggle and verify it switches between light/dark modes.
- Check that header layout is balanced and space-efficient.
- Ensure logo displays correctly and actions are accessible.

## Risks
- Moving JS could break if not handled properly, but the logic is simple.
- Logo image URL needs to be valid (user provided URL).
- Simplified toggle may be less clear to users, but icon-only is common in headers.

## Questions for User
- Is the logo URL correct and should it be cached locally?
- Should the toggle have any hover states or animations for better UX?
- Any specific spacing or sizing preferences for the header elements?</content>
<parameter name="filePath">C:\Users\Ivan Gonzalez\Sitios\loveshack\.kilo\plans\1778086743889-silent-wolf.md